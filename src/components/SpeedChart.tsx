// Reading-speed curve — median recognition time over time (the hero metric visual).
import React from 'react';
import { Text, View } from 'react-native';

export default function SpeedChart(): React.ReactElement {
  return <View><Text>SpeedChart</Text></View>;
}
