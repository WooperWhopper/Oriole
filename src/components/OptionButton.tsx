// Tappable romanisation option in the drill.
import React from 'react';
import { Text, TouchableOpacity } from 'react-native';

export default function OptionButton(): React.ReactElement {
  return <TouchableOpacity><Text>OptionButton</Text></TouchableOpacity>;
}
